# ---------------------------------------------------------------------------
# Gravitas Majlis — environment template
#
# Copy to .env and fill in. .env is gitignored and must never be committed.
# The repository is public: a key committed here is a key that is published.
# ---------------------------------------------------------------------------

# Anthropic API key for the comprehension assistant.
# Set this in the hosting platform's secret store in production, not in a file.
ANTHROPIC_API_KEY=sk-ant-...

# Models.
#
# The defaults in the code are these, and this file drifted a generation
# behind them once already — an override here silently pinning a retired
# model looks exactly like an override somebody meant.
#
# EXTRACTION_MODEL reads figures out of a document; it has the same default
# as the assistant and was missing from this file entirely.
ASSISTANT_MODEL=claude-opus-5
EXTRACTION_MODEL=claude-opus-5
CLASSIFIER_MODEL=claude-haiku-4-5-20251001

# Speaking a reason instead of typing it.
#
# Off unless you set it, and off is the honest default: the browser sends the
# recording away to be transcribed, which is a decision an institution makes
# rather than one an application makes for it. Where it is off the interface
# says so in place rather than showing nothing, because the screen a member
# was shown has the control on it.
#
# MAJLIS_DICTATION=browser

# --- Whose record this is ---------------------------------------------------
#
# MAJLIS_INSTITUTION
#   Which institution this service serves. Every board belongs to one, and
#   isolation is enforced at the store boundary rather than in the routes, so a
#   route cannot reach another institution even by mistake.
#
#   Left unset where the record holds exactly one institution, it uses that.
#   Where the record holds several and this does not say which, the service
#   REFUSES TO START — serving unscoped data across institutions is the single
#   failure that boundary exists to prevent, and a service that starts and then
#   leaks is worse than one that does not start.
#
#   A single deployment per institution is still the arrangement a bank will
#   ask for. This makes the record correct either way.
#
# MAJLIS_INSTITUTION=demo-institution

# --- What this installation has attached ------------------------------------
#
# Both default to nothing, and nothing is a complete installation. A Shariah
# board inside a bank runs with neither: the record, deliberation, evidence,
# operative terms, voting and export all work with no chain and no assistant.
#
# MAJLIS_ENFORCEMENT   none | gravitas-registry
#   What carries out what the board decides. Nothing here ever performs
#   enforcement — an adapter reports what the enforcing system currently says,
#   so a board can see whether what runs matches what it approved.
#   Left unset, it is inferred from POLICY_REGISTRY_ADDRESS so an installation
#   configured before this existed does not lose its chain read on upgrade.
#
# MAJLIS_ASSISTANT     off | anthropic
#   The comprehension assistant sends the text of a member's question to a model
#   run by someone else. Some institutions forbid that outright, and a board's
#   deliberation is among the most sensitive text an institution holds.
#   Left unset, it is inferred from ANTHROPIC_API_KEY. Set it to off explicitly
#   where policy requires the guarantee rather than the default.
#
# MAJLIS_ENFORCEMENT=none
# MAJLIS_ASSISTANT=off

# --- Limits -----------------------------------------------------------------
# The assistant endpoint sits behind the same authentication as everything else;
# only /api/health is exempt. Authentication says who may call it, not how often,
# so these limits are what stands between an authenticated caller and the API
# budget. Do not deploy without them.
RATE_LIMIT_PER_IP=10
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_GLOBAL_PER_DAY=2000
# Hard ceiling on estimated model spend per UTC day, in USD. The endpoint stops
# when this is reached; it does not warn and continue.
ASSISTANT_DAILY_USD_CAP=20
# Estimated cost of one question (classifier call + assistant call).
ASSISTANT_USD_PER_QUESTION=0.02

# --- who is at the keyboard --------------------------------------------------
# An entry is memberId:role:secret, or institution/memberId:role:secret where
# the credential should name whose it is. A service serving one institution
# refuses a credential naming another at the door, rather than admitting them
# to a record they cannot see.
#
# One credential per member, so a vote can be attributed to whoever cast it.
# Roles: signatory (votes and objects), advisory and liaison (deliberate only),
# observer (reads), institution (the bank's own desk).
#
# Generate a line, typing the password at a prompt rather than passing it as an
# argument, which would land in the shell history:
#   npm run member -w server -- member-a signatory
#
# A password identifies. It does not sign. Stage Three is where the vote becomes
# a signature under a key the scholar controls.
#
# --- Give the bank a credential of its own ----------------------------------
#
# The institution's desk needs one too, and for a long time no installation had
# one: not the seed, not this file, not DEMO.md. The role existed in the code
# and nothing could sign in under it, so the bank's whole side of the product
# had never been opened by anybody. What it turned out to be showing was the
# board's entire navigation — the triage queue, the sittings, the matters in
# hand — with the acts correctly absent and the instructions left behind.
#
#   npm run member -w server -- desk-1 institution
#
# A desk sees three doors of its own — what it asked, what binds it, what it
# owes — and is told plainly when it has landed on a screen that is the
# board's. Without one of these lines, none of that is reachable and the
# demonstration silently shows only half the product.
MAJLIS_MEMBERS=

# The shared credential. It cannot say which member is here, so a request
# authenticated with it is an observer: it reads, and writes nothing. Set
# MAJLIS_MEMBERS instead once the board is real. Generate the password with:
#   openssl rand -base64 18
BASIC_AUTH_USER=board
BASIC_AUTH_PASSWORD=

# --- The record -------------------------------------------------------------
# Where the board record is kept. A JSON file, written atomically.
#
# Leave it unset and the record lives in memory and dies with the process, which
# is right for a development run and wrong for anything else. In production the
# server refuses to start without it rather than silently forgetting decisions.
#
# On a host with an ephemeral filesystem this must point at a mounted volume.
# A path in the working directory looks like it works and loses everything on
# the next deploy.
MAJLIS_DB=./data/majlis.json

# Where a scholar's uploaded document goes.
#
# Unset, files go into a `files` directory beside MAJLIS_DB — the same volume,
# which is the one place already known to survive. Set this only to put them
# somewhere else.
#
# With neither set there is no vault, and the server refuses to take a document
# rather than accepting one into storage that does not survive a restart: the
# board would cite it, and the citation would point at nothing. /api/health
# reports `documents` so the interface stops offering an upload it cannot keep.
# MAJLIS_FILES=./data/files

# Whether an attached document may be read by a model.
#
# Off unless this says otherwise, and never inferred from a key being present.
# The assistant infers from an existing key so that an upgrade does not remove
# something an installation was using; nothing here has ever been on, and a
# bank's financial statements are not something to start sending by default.
#
# It is a separate switch from MAJLIS_ASSISTANT on purpose. Sending a member's
# question to a model is one decision an institution has to make; sending the
# bank's accounts is a larger one, and allowing the first does not allow the
# second.
#
# Off, the figures are typed in — which is what they always were, and why this
# can be off without the application being degraded.
# MAJLIS_READING=anthropic

# --- Server -----------------------------------------------------------------
PORT=4000

# --- Chain ------------------------------------------------------------------
RPC_URL=https://sepolia-rollup.arbitrum.io/rpc

# Deployed Gravitas contracts (Arbitrum Sepolia).
# Redeployed and verified on Arbiscan, 23 August 2026. The bytecode at these
# addresses is built from the source in this repository. See docs/DEPLOYMENTS.md.
POLICY_REGISTRY_ADDRESS=0x6f3bfb896DD9964C9c05dA88692bDf1b1b2C3F23
TELEPORT_V3_ADDRESS=0x6702C2CE6eD58ca3934eBBd785CaC1De8DCd85B4

# Set to "true" to serve seed data only and never touch the chain.
OFFLINE_MODE=false
